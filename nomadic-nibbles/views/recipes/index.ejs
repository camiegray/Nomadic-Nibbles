<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Recipes</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/nav') %>
  <main>
    <h1>All Recipes</h1>
    <ul>
      <% recipes.forEach(recipe => { %>
        <li>
          <a href="/recipes/<%= recipe._id %>"><%= recipe.title %></a>
          by <%= recipe.author.username %> (<%= recipe.region.name %>)
        </li>
      <% }) %>
    </ul>
    <% if (user) { %>
      <a href="/recipes/new" class="btn">Create New Recipe</a>
    <% } %>
  </main>
</body>
</html>
