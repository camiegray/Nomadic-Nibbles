<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Nomadic Nibbles</title>
  <style>
    /* Basic Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      color: #333;
      line-height: 1.6;
    }
    /* Navbar Styles */
    .navbar {
      background: rgba(51, 51, 51, 0.8);
      color: #fff;
      padding: 10px 20px;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000;
    }
    .navbar .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .navbar .logo {
      font-size: 24px;
      font-weight: bold;
      color: #F4A261;
      text-decoration: none;
    }
    .nav-links a {
      color: #fff;
      text-decoration: none;
      margin-left: 15px;
      font-size: 16px;
    }
    .nav-links a.btn {
      padding: 5px 10px;
      border-radius: 4px;
    }
    .nav-links a.login {
      background-color: #F4A261;
    }
    .nav-links a.signup {
      background-color: #2A9D8F;
    }
    /* Hero Section Styles */
    .hero {
      height: 100vh;
      background: url(https://i.imgur.com/MJk1Jht.jpeg) no-repeat center center/cover;
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      margin-top: 60px; /* offset for fixed navbar */
    }
    .hero-overlay {
      background: rgba(0, 0, 0, 0.4);
      padding: 20px;
      text-align: center;
      border-radius: 8px;
    }
    .hero h1 {
      font-size: 48px;
      color: #fff;
      margin-bottom: 10px;
    }
    .hero p {
      font-size: 20px;
      color: #fff;
      margin-bottom: 20px;
    }
    .hero .btn {
      display: inline-block;
      padding: 10px 20px;
      margin: 0 10px;
      font-size: 18px;
      text-decoration: none;
      border-radius: 4px;
    }
    .hero .btn.explore {
      background-color: #F4A261;
      color: #fff;
    }
    .hero .btn.search {
      background-color: #2A9D8F;
      color: #fff;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <header class="navbar">
    <div class="container">
      <a href="/" class="logo">Nomadic Nibbles</a>
      <nav class="nav-links">
        <a href="/">Home</a>
        <a href="/recipes">Recipes</a>
        <a href="/users/login" class="btn login">Login</a>
        <a href="/users/signup" class="btn signup">Sign Up</a>
      </nav>
    </div>
  </header>
  <!-- Hero Section -->
  <section class="hero">
    <div class="hero-overlay">
      <h1>Savor the Flavors of the World</h1>
      <p>Discover and share global recipes with a cultural twist.</p>
      <a href="/recipes" class="btn explore">Explore Recipes</a>
      <a href="/recipes/search" class="btn search">Recipe Search</a>
    </div>
  </section>
</body>
</html>
